schema_version: 1

context:
  name: git-remote-s3
  version: "0.4.1"
  python_min: '3.10'

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/git_remote_s3-${{ version }}.tar.gz
  sha256: efa2749e25663d8faa792cf3e94725f6452a441f21a0ed564ff4025ef160fb39

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - git-remote-s3 = git_remote_s3.remote:main
      - git-lfs-s3 = git_remote_s3.lfs:main
      - git-s3 = git_remote_s3.manage:main

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}
    - botocore >=1.42.24,<2.0.0
    - boto3 >=1.42.24,<2.0.0
    - urllib3 >=2.6.3,<3.0.0

tests:
  - python:
      imports:
        - git_remote_s3
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
    script:
      - git-s3 --help

about:
  summary: A git remote helper for Amazon S3
  license: Apache-2.0
  license_file:
    - LICENSE
    - NOTICE
  homepage: https://github.com/awslabs/git-remote-s3

extra:
  recipe-maintainers:
    - thewchan
